services:
  agro-price-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agro-price-forecast-api
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./models:/app/models
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
      - ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload